<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><img class="header-logo" src="{{ asset('img/logo.png') }}" alt="logo">Bucket-List</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('wish_index') }}">All wishes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_index') }}">Categories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_about') }}">About us</a>
                </li>
            </ul>

            {% if app.user %}
                <form action="{{ path('app_logout') }}" method="post" class="d-flex">
                    <input type="text" placeholder="pseudo" value="Hello {{ app.user.userIdentifier }}" readonly="readonly" name="pseudo" id="inputPseudo" class="form-control-sm mx-2" autocomplete="username" required autofocus>
                    <button class="btn btn-lg btn-primary btn-sm" type="submit">logout</button>
                </form>
            {% else %}
                <form action="{{ path('app_login') }}" method="post" class="d-flex form-inline">
                    <label for="inputPseudo" class="visually-hidden">Pseudo</label>
                    <input type="text" placeholder="pseudo" {% if last_username|default(false) %} value="{{ last_username }}" {% endif %} name="pseudo" id="inputPseudo" class="form-control-sm" autocomplete="username" required autofocus>
                    <div class="invalid-feedback">Invalid pseudo or password</div>
                    <label for="inputPassword" class="visually-hidden">Password</label>
                    <input type="password" placeholder="password" name="password" id="inputPassword" class="form-control-sm mx-2" autocomplete="current-password" required>
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    <button class="btn btn-lg btn-primary btn-sm mx-2" type="submit">login</button>
                    <a href="{{ path('app_register') }}"><button class="btn btn-lg btn-secondary btn-sm" type="button">register</button></a>
                </form>
            {% endif %}
        </div>
    </div>
</nav>
